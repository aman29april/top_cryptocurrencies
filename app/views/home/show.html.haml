%h4.text-center Top Cryptocurrencies
%table.table
  %thead
    %tr
      %th #
      %th Currency Code
      %th Currency Name
      %th Market Cap (USD)
      %th Value (USD)
  %tbody
    - @currencies.each do |c|
      %tr
        %td
          = c['id']
        %td
          = c['symbol']
        %td
          = c['name']
        %td
          = number_to_currency c['quote']['USD']['price']
        %td
          = number_to_currency c['quote']['USD']['market_cap']
